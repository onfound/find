package com.company;

import org.junit.Test;

import static org.junit.Assert.*;

public class FindTest {
    Find file1 = new Find(true);
    Find file2 = new Find(false);
    @Test
    public void find(){
       /* assertEquals("File is find", file2.find("testFile.txt", System.getProperty("user.dir")));*/
    }
}